#spring:
#  datasource:
#    platform: h2
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore_dev?serverTimezone=UTC
    username: bookstore_dev_user
    password: dev
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
  thymeleaf:
    cache: false
